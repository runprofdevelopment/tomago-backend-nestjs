const COLLECTION_NAME = 'decoopa-account';
const DEFAULT_ACCOUNT_ID = 'default';

const DECOOPA_ACCOUNT = {
  id: DEFAULT_ACCOUNT_ID,
  balance: 0,
  currency: 'EGP',
};

module.exports = {
  COLLECTION_NAME,
  DEFAULT_ACCOUNT_ID,
  DECOOPA_ACCOUNT,
};